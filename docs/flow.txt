# Basic work flows for pacman project:

# prevent accidentally sourcing the commands in this file:
return

# connecting to UART
busybox microcom -s 115200 /dev/ttyUSB1
# (Use Ctrl-X to exit)

# building from repo:
git clone https://github.com/mulhearn/pacman.git
# or, with ssh:
git clone git@github.com:mulhearn/pacman.git
cd pacman
source /tools/Xilinx/Vivado/2020.2/settings64.sh
# create the project from tcl
vivado -mode batch -source vivado/tcl/create.tcl
# run systhesis, implementation, and write bit stream:
vivado -mode batch -source vivado/tcl/runsib.tcl

# run hello world demo:
xsct apps/hello/install.tcl
cd hello
xsct run.tcl

# run hwcheck
xsct apps/hwcheck/install.tcl
cd hwcheck
xsct run.tcl
#  Run "check RO registers" test to read these out.


# install hello world to qspi flash demo (also shows creating BOOT.bin with bootgen)
xsct apps/flash/install.tcl
# you can now copy flash/BOOT.bin onto the BOOT partition of an SD card to boot hello world from SD card, and/or:
# write BOOT.bin to QSPI flash:
cd flash
source program.sh
# switch to QSPI boot mode (boots hello world)

# petalinux ramdisk (uses RAM for root fs)
source /tools/Xilinx/PetaLinux/2022.2/bin/settings.sh
cd petalinux
scripts/create_trenz.sh
cd trenz
../scripts/config.sh
../scripts/build.sh
../scripts/package.sh
cd images/linux
ls -alh BOOT.BIN image.ub boot.scr
# (Don't forget to run package script to update BOOT.BIN!)

# put those files on partition 1 (vfat) of an SD card,
# insert in module and boot to Linux...
#

# Read out the AXI-Lite secondary address (read only registers) via devmen:

# NOTE:  starting address was noted by reading the address editor in Vivado for the block diagram.

root@Trenz:~# /sbin/devmem 0x40000000     
0x00000000
root@Trenz:~# /sbin/devmem 0x40000004
0x00000000
root@Trenz:~# /sbin/devmem 0x40000008
0x11111111
root@Trenz:~# /sbin/devmem 0x4000000C
0x22222222
root@Trenz:~# /sbin/devmem 0x40000000 32 0xABCDFFFF
root@Trenz:~# /sbin/devmem 0x40000004 32 0x77777777
root@Trenz:~# /sbin/devmem 0x40000000              
0xABCDFFFF
root@Trenz:~# /sbin/devmem 0x40000004
0x77777777

Alignment trap: devmem (681) PC=0x004911b4 Instr=0x59eb Address=0xb6f71015 FSR 0x011
0xFFFFFFFF

# building just the hwutil (or pacman-server):


../scripts/build_hwutil.sh

# Convert the RPM from into a gzipped tar file:
mkdir tmp
cd tmp
rpm2cpio ../hwutil-latest.rpm | cpio -idmv
ls
tar -czvf hwutil-latest.tar.gz * 
mv hwutil-latest.tar.gz ..
cd ..
rm -fr tmp

